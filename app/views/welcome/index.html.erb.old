<div class="page-header">
  <h1>Lançamentos </h1>
</div>

<div class="row">

  <% @feeds.each do |lancamento| %>
  <div class="well">
    <p><strong><%= "#{lancamento.title} - #{time_ago_in_words lancamento.moment}" %></strong></p>
    <%= link_to image_tag(lancamento.image), lancamento.url, target: :blank %>
  </div>
  <% end %>
</div>
